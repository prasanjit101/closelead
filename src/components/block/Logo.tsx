import { Merge } from "lucide-react";

export const Logo = () => {
  return (
    <div className="flex items-center gap-2 font-semibold text-primary">
      <Merge />
      closelead
    </div>
  );
};
