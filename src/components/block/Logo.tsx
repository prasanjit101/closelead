import { Merge } from "lucide-react";

export const Logo = () => {
  return (
    <div className="font-semibold flex items-center gap-2 text-primary">
      <Merge />
      supamanager
    </div>
  );
};